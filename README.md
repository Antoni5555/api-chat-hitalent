# API чатов и сообщений

REST API сервис для создания чатов и сообщений, реализованный на Django + Django REST Framework.  
Проект выполнен в рамках тестового задания и полностью соответствует предъявленным требованиям:  
валидация входных данных, каскадное удаление, работа через ORM, PostgreSQL, Docker, docker-compose и тесты.

---

## Функциональность

- Создание чатов
- Отправка сообщений в чат
- Получение чата с последними N сообщениями
- Удаление чата вместе со всеми сообщениями

---

## Архитектура

Проект построен по принципам модульности и разделения ответственности:

- **models** — модели Chat и Message
- **serializers** — валидация входных данных и формирование ответов API
- **views** — бизнес-логика API
- **urls / routers** — маршрутизация
- **tests** — pytest тесты API
- **migrations** — миграции базы данных

Связь моделей:

```
Chat 1 ---- N Message
```

Каскадное удаление сообщений реализовано на уровне ORM и БД.

---

## 🛠 Стек

- Python 3.12
- Django + Django REST Framework
- PostgreSQL
- Docker + docker-compose
- Pytest

---

## Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repo_url>
cd hitalent
```

### 2. Запуск через Docker

```bash
docker-compose up --build
```

Проект будет доступен по адресу:

```
http://localhost:8000
```

### 3. Применение миграций

В отдельном терминале:

```bash
docker-compose exec web python manage.py migrate
```

### 4. Создание суперпользователя (опционально)

```bash
docker-compose exec web python manage.py createsuperuser
```

### 5. Запуск тестов

```bash
docker-compose exec web pytest
```

---

## API Документация

### Создать чат

**POST /chats/**

Body:

```json
{
  "title": "Мой чат"
}
```

Response:

```json
{
  "id": 1,
  "title": "Мой чат",
  "created_at": "2026-01-29T12:00:00Z"
}
```

---

### Отправить сообщение

**POST /chats/{id}/messages/**

Body:

```json
{
  "text": "Привет! Мой друг."
}
```

Response:

```json
{
  "id": 10,
  "chat_id": 1,
  "text": "Привет! Мой друг.",
  "created_at": "2026-01-29T12:01:00Z"
}
```

Если чат не существует → **404 Not Found**

---

### Получить чат и последние сообщения

**GET /chats/{id}?limit=20**

Query params:

- `limit` — количество последних сообщений (по умолчанию 20, максимум 100)

Response:

```json
{
  "chat": {
    "id": 1,
    "title": "Мой чат",
    "created_at": "2026-01-29T12:00:00Z"
  },
  "messages": [
    {
      "id": 10,
      "chat_id": 1,
      "text": "Привет! Мой друг.",
      "created_at": "2026-01-29T12:01:00Z"
    }
  ]
}
```

Сообщения отсортированы по `created_at`.

---

### Удалить чат

**DELETE /chats/{id}**

Response:

```
204 No Content
```

При удалении чата все сообщения удаляются каскадно.

---

## Валидация и ограничения

### Chat.title

- не пустой
- длина 1..200
- пробелы по краям триммируются

### Message.text

- не пустой
- длина 1..5000

### Ограничения

- Нельзя отправить сообщение в несуществующий чат → **404**
- limit для получения сообщений: максимум 100

---

## Тестирование

В проекте используются **pytest** тесты для проверки:

- создания чатов
- отправки сообщений
- получения чата с сообщениями
- ошибок валидации
- работы каскадного удаления

Запуск:

```bash
docker-compose exec web pytest
```

---

## Миграции

Все изменения моделей оформлены через Django migrations:

```bash
docker-compose exec web python manage.py makemigrations
docker-compose exec web python manage.py migrate
```

---

## Соответствие ТЗ

| Требование | Реализовано |
|------------|-------------|
| Django / FastAPI | ✅ Django + DRF |
| ORM | ✅ Django ORM |
| PostgreSQL | ✅ |
| Docker + docker-compose | ✅ |
| Миграции | ✅ |
| Тесты | ✅ pytest |
| Валидация данных | ✅ |
| Каскадное удаление | ✅ |
| README с инструкцией | ✅ |

---

## Комментарии по архитектуре

- Бизнес-логика вынесена во views и serializers
- Отдельные сериализаторы для create / retrieve
- Используется атомарность операций
- Код разделён по слоям ответственности
- Лёгкая масштабируемость под WebSocket или async-архитектуру

---

## Автор

Проект выполнен в рамках тестового задания на позицию backend-разработчика.
